# 專案工作指引

## 開始任務

- 先讀 [專案記憶](docs/project-memory.md)，再依需求讀相關程式與來源文件；不必每次重新全面掃描。
- 使用者可以直接用繁體中文描述需求。根據既有功能與下方文件定位實作；只有影響結果的重要歧義才需要釐清。
- 先檢查工作目錄變更，保留使用者尚未提交的內容。
- 記憶是導覽，程式碼與本次明確需求才是判斷依據。不要把已知問題當成必須維持的規格。

## 專案原則

- 這是 Go 標準函式庫實作的本機 AI 用量面板。沿用單一執行檔及內嵌 HTML/CSS/JavaScript；一般功能修改不需引入前端建置工具或資料庫。
- 保持分層：`model` 是資料規則，`service` 是統計與協調，`source` 是來源 IO，`controller` 是 HTTP，`view` 是呈現，`app` 負責組裝。
- `service` 不加入供應商 ID 分支；來源差異透過 `Collector` registry 與 `ProviderPolicy` 表達。
- 帳號回報百分比、本機 token 與手動用量要分開解讀；無資料不可呈現為已用 0%。
- 不把登入 token、面板含 token 網址、個人對話或本機帳號資料寫入程式、測試案例或記憶文件。測試使用合成資料與暫存目錄。
- 調整 HTTP、掃描或視窗時，保留本機驗證、SSE 連線生命週期與設定儲存成功後才生效的行為。

## 驗證與交付

- Go 入口為 `./cmd/aiusage`。一般程式變更執行相關測試與 `go test ./...`；並行或跨層變更執行 `go test -race ./...`、`go vet ./...`。
- 主機沒有 Go 時可用 Docker，指令見專案記憶與 README；平台專用修改需另外驗證對應平台，Linux 測試不能取代 Windows 實機驗證。
- 純文件變更檢查內容、連結及差異，不必重跑完整建置。不要為了測試啟動真實帳號查詢。
- 完成後以繁體中文說明結果、驗證及未解決限制。
- 程式註解保持精簡，只解釋必要的非直覺行為；詳細設計與操作說明放在文件。
- 若功能、入口、資料流、設定、建置方式或限制改變，同步更新 `docs/project-memory.md`；使用方式改變也更新 README / 來源文件。
- 檢視發現與當次測試結果另記於 [檢視紀錄](docs/review-2026-09-14.md)，避免與長期規格混淆。
