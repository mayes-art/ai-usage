# 專案檢視紀錄：2026-09-14

程式基準：`6133495`。範圍為架構、設定、統計、來源收集、HTTP 與前端互動的程式碼檢視，並執行既有測試；未使用個人帳號實測第三方服務。此次交付為記憶與檢視文件，以下程式問題尚未修改。

## 待修正發現

### [P2] 儲存失敗後仍關閉上限表單

位置：[internal/view/ui.html](../internal/view/ui.html)，`buildLimitForm` 的儲存處理（檢視時第 610–616 行）。

若設定目錄無法寫入，後端會回傳 HTTP 500，Store 正確保留舊設定；但前端 `await fetch(...)` 沒有檢查 `response.ok`。HTTP 錯誤仍會走到 `editing = null` 並刷新，讓使用者失去剛輸入的內容且不知道儲存失敗。網路例外也沒有就地錯誤提示。

建議：檢查回應狀態，失敗時保留表單與輸入並顯示錯誤，只有成功才結束編輯。驗收需涵蓋正常儲存、HTTP 500 及網路失敗。此結論來自程式碼路徑檢視，尚未進行瀏覽器互動重現。

### [P2] 上限 API 接受負數並把不存在的來源回報成功

位置：[internal/controller/server.go](../internal/controller/server.go)，`handleLimit`（檢視時第 188–218 行）；前端數值輸入在 `internal/view/ui.html` 的 `buildLimitForm`。

`handleLimit` 解碼後直接寫入 `limit` / `used`，未拒絕負數；來源不存在時 callback 直接返回，但外層仍儲存並回傳 `ok: true`。例如已驗證請求送出 `{"provider":"codex","limit":-1}` 會保存無效上限；Cursor 的負數已用量也會被保存。前端 `min=0` 無法單獨防止此情況，因為儲存是 `type=button` 並直接呼叫 fetch，未執行表單有效性檢查。

建議：更新前驗證來源、非負且有限的數值與合理的視窗範圍，保留 `limit=0` 清除手動限制的既有語意；驗證失敗回傳 4xx 且不寫設定。補上負數、未知來源、正常值及清除限制的測試。此結論來自程式碼路徑檢視，尚未新增重現測試。

## 已有文件揭露的資料邊界

- [internal/service/store.go](../internal/service/store.go)：累計鍵為來源 ID + session；session 為空時，不同工作階段可能互相影響。
- [internal/record/scan.go](../internal/record/scan.go)：以秒級修改時間和大小判定不變，同秒等大小覆寫可能漏讀。
- 「改用手動上限」尚未關閉 `prefer_reported_limits`，回報值可繼續優先；README 已揭露此介面限制。

以上為已知邊界，與本次文件修改無關。

## 驗證結果

- 以 Docker Linux 容器、唯讀掛載專案執行，工具鏈實際輸出為 `go1.27.1 linux/amd64`。
- `go test -race ./...`：全部通過（14 個有測試的套件）。
- `go vet ./...`：通過，命令退出碼 0。
- 未執行 Windows/macOS 原生測試、瀏覽器互動測試或第三方帳號連線驗證，也未重新產出發行執行檔。
- 既有測試通過不代表覆蓋上述錯誤情境；兩項發現仍待修正與驗收。

## 追加：Cursor 方案總百分比固定 100%（已修正）

實機驗證面板時發現 Cursor 主列顯示 `100% US$20.00 / US$20.00`，與 Cursor 本身的顯示不符。
以暫時的原始回應 dump（在專案外的副本上進行，未改動工作目錄）核對 `api2.cursor.sh` 回傳：

```
totalSpend 3624, includedSpend 2000, bonusSpend 1624, limit 2000,
totalPercentUsed 14.496, autoPercentUsed 0.138, apiPercentUsed 89.875
"You've used 14% of your included total usage"
"You've used 90% of your included API usage"
```

`includedSpend` 是方案內含額度而非已用金額（`2000 + 1624 = 3624`），因此
`parseCursorUsage` 原本的 `includedSpend / limit` 恆等於 100%，與實際用量無關；
`math.Min(100, …)` 也會把超額壓成 100%。兩個模型池的百分比本來就正確。

修正後 `parseCursorUsage` 一律採用 `totalPercentUsed`，金額改由百分比乘上 `limit` 換算，
並移除 100% 截斷；沒有 `totalPercentUsed` 時方案總量視為未知，僅有分項時仍可顯示分項。
`internal/source/cursor/cursor.go` 的 `IncludedSpend` 欄位一併移除。

新增測試：`allowance fields are not usage`、`overage is not clamped`、額度欄位無百分比必須報錯、
實機形狀（`includedSpend == limit == 2000` 且 `totalPercentUsed` 14.496）必須得到 14.496% 與 US$2.90，
以及 `groups_test.go` 的 `pools without a reported total`、`live shape: low total, exhausted other`。
已確認這些測試在修正前的程式上會失敗、修正後通過。

驗證：Docker Linux（go1.27.1）`go vet ./...` 與 `go test -race ./...` 全部通過；
Windows amd64 交叉建置後實機 `report` 顯示 `Cursor IDE / CLI 14% US$2.90 / US$20.00`，
子項目 `Cursor model: 0%`、`Other model: 90%`，與 Cursor 自己的訊息一致。
`doctor` 與面板 `/api/snapshot` 亦正常回應。未驗證 macOS 與 Linux 實機。
